/***** THIS FILE IS AUTOGENERATED DO NOT MODIFY DIRECTLY ! *****/
extension iopsq__field__array_t : libcommon.IopArray { }
extension iopsq__enum_val__array_t : libcommon.IopArray { }
extension iopsq__package_elem__array_t : libcommon.IopArray { }
extension iopsq__int_size__t : libcommon.IopEnum {
    public static let descriptor = iopsq__int_size__ep
    public static let min : Swift.Int32 = Swift.Int32(INT_SIZE_min)
    public static let max : Swift.Int32 = Swift.Int32(INT_SIZE_max)
    public static let count : Swift.Int32 = Swift.Int32(INT_SIZE_count)
}
extension iopsq__string_type__t : libcommon.IopEnum {
    public static let descriptor = iopsq__string_type__ep
    public static let min : Swift.Int32 = Swift.Int32(STRING_TYPE_min)
    public static let max : Swift.Int32 = Swift.Int32(STRING_TYPE_max)
    public static let count : Swift.Int32 = Swift.Int32(STRING_TYPE_count)
}
public enum iopsq : libcommon.IopPackage {
    public typealias IntSize = iopsq__int_size__t

    public typealias StringType = iopsq__string_type__t

    public final class IntType : libcommon.IopStruct {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__int_type__sp
        }

        public var `isSigned` : Swift.Bool
        public var `size` : iopsq_package.IntSize

        public init(`isSigned`: Swift.Bool,
                    `size`: iopsq_package.IntSize) {
            self.isSigned = `isSigned`
            self.size = `size`
            super.init()
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__int_type__t.self, capacity: 1)
            self.isSigned = data.pointee.is_signed
            self.size = data.pointee.size
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__int_type__t.self, capacity: 1)
            data.pointee.is_signed = self.isSigned
            data.pointee.size = self.size
        }
    }

    public indirect enum IopType : libcommon.IopUnion {
        public static let descriptor =  iopsq__iop_type__sp

        case `i`(iopsq_package.IntType)
        case `b`
        case `d`
        case `s`(iopsq_package.StringType)
        case `v`
        case `typeName`(Swift.String)
        case `array`(iopsq_package.IopType)

        public init(_ c: Swift.UnsafeRawPointer) throws {
            let (tag, data) = type(of: self)._explode(c)
            switch tag {
              case 1:
                self = .i(try iopsq_package.IntType(data))
              case 2:
                self = .b
              case 3:
                self = .d
              case 4:
                self = .s(data.bindMemory(to: iopsq_package.StringType.self, capacity: 1).pointee)
              case 5:
                self = .v
              case 6:
                self = .typeName(Swift.String(data.bindMemory(to: LString.self, capacity: 1).pointee) ?? "")
              case 7:
                self = .array(try iopsq_package.IopType(data.bindMemory(to: Swift.UnsafeRawPointer.self, capacity: 1).pointee))
              default:
                throw libcommon.IopImportError.invalidUnionTag(type(of: self).descriptor, tag)
            }
        }

        public func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let (tag, data) = type(of: self)._explode(c)
            switch self {
              case .i(let i_val):
                tag[0] = 1
                i_val.fill(data, on: allocator)
              case .b:
                tag[0] = 2
              case .d:
                tag[0] = 3
              case .s(let s_val):
                tag[0] = 4
                data.bindMemory(to: iopsq_package.StringType.self, capacity: 1).pointee = `s_val`
              case .v:
                tag[0] = 5
              case .typeName(let typeName_val):
                tag[0] = 6
                data.bindMemory(to: libcommon.LString.self, capacity: 1).pointee = typeName_val.duplicated(on: allocator)
              case .array(let array_val):
                tag[0] = 7
                data.bindMemory(to: Swift.UnsafeMutableRawPointer.self, capacity: 1).pointee = array_val.duplicated(on: allocator)
            }
        }
    }

    public class PackageElem : libcommon.IopClass {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__package_elem__sp
        }

        open override class var isAbstract : Swift.Bool {
            return true
        }

        public var `name` : Swift.String

        public init(`name`: Swift.String) {
            self.name = `name`
            super.init()
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__package_elem__t.self, capacity: 1)
            self.name = Swift.String(data.pointee.name) ?? ""
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__package_elem__t.self, capacity: 1)
             data.pointee.name = self.name.duplicated(on: allocator)
        }
    }

    public enum Value : libcommon.IopUnion {
        public static let descriptor =  iopsq__value__sp

        case `i`(Swift.Int64)
        case `u`(Swift.UInt64)
        case `d`(Swift.Double)
        case `s`(Swift.String)
        case `b`(Swift.Bool)

        public init(_ c: Swift.UnsafeRawPointer) throws {
            let (tag, data) = type(of: self)._explode(c)
            switch tag {
              case 1:
                self = .i(data.bindMemory(to: Swift.Int64.self, capacity: 1).pointee)
              case 2:
                self = .u(data.bindMemory(to: Swift.UInt64.self, capacity: 1).pointee)
              case 3:
                self = .d(data.bindMemory(to: Swift.Double.self, capacity: 1).pointee)
              case 4:
                self = .s(Swift.String(data.bindMemory(to: LString.self, capacity: 1).pointee) ?? "")
              case 5:
                self = .b(data.bindMemory(to: Swift.Bool.self, capacity: 1).pointee)
              default:
                throw libcommon.IopImportError.invalidUnionTag(type(of: self).descriptor, tag)
            }
        }

        public func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let (tag, data) = type(of: self)._explode(c)
            switch self {
              case .i(let i_val):
                tag[0] = 1
                data.bindMemory(to: Swift.Int64.self, capacity: 1).pointee = `i_val`
              case .u(let u_val):
                tag[0] = 2
                data.bindMemory(to: Swift.UInt64.self, capacity: 1).pointee = `u_val`
              case .d(let d_val):
                tag[0] = 3
                data.bindMemory(to: Swift.Double.self, capacity: 1).pointee = `d_val`
              case .s(let s_val):
                tag[0] = 4
                data.bindMemory(to: libcommon.LString.self, capacity: 1).pointee = s_val.duplicated(on: allocator)
              case .b(let b_val):
                tag[0] = 5
                data.bindMemory(to: Swift.Bool.self, capacity: 1).pointee = `b_val`
            }
        }
    }

    public final class OptInfo : libcommon.IopStruct {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__opt_info__sp
        }

        public var `defVal` : iopsq_package.Value?

        public init(`defVal`: iopsq_package.Value? = nil) {
            self.defVal = `defVal`
            super.init()
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__opt_info__t.self, capacity: 1)

            self.defVal = try iopsq_package.Value(data.pointee.def_val)
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__opt_info__t.self, capacity: 1)
            if let defVal_val = self.defVal {
                data.pointee.def_val = defVal_val.duplicated(on: allocator).bindMemory(to: iopsq__value__t.self, capacity: 1)
            }
        }
    }

    public final class Field : libcommon.IopStruct {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__field__sp
        }

        public var `name` : Swift.String
        public var `type` : iopsq_package.IopType
        public var `optional` : iopsq_package.OptInfo?
        public var `tag` : Swift.UInt16?
        public var `isReference` : libcommon.IopVoid?

        public init(`name`: Swift.String,
                    `type`: iopsq_package.IopType,
                    `optional`: iopsq_package.OptInfo? = nil,
                    `tag`: Swift.UInt16? = nil,
                    `isReference`: libcommon.IopVoid? = nil) {
            self.name = `name`
            self.type = `type`
            self.optional = `optional`
            self.tag = `tag`
            self.isReference = `isReference`
            super.init()
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__field__t.self, capacity: 1)
            self.name = Swift.String(data.pointee.name) ?? ""

            var type_var = data.pointee.type
            self.type = try iopsq_package.IopType(&type_var)
            

            self.optional = try iopsq_package.OptInfo(data.pointee.optional)
            if data.pointee.tag.has_field {
                self.tag = data.pointee.tag.v
            }
            if data.pointee.is_reference {
                self.isReference = libcommon.IopVoid()
            }
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__field__t.self, capacity: 1)
             data.pointee.name = self.name.duplicated(on: allocator)
            data.pointee.type = self.type.duplicated(on: allocator).bindMemory(to: iopsq__iop_type__t.self, capacity: 1).pointee
            if let optional_val = self.optional {
                data.pointee.optional = optional_val.duplicated(on: allocator).bindMemory(to: iopsq__opt_info__t.self, capacity: 1)
            }
            if let tag_val = self.tag {
                data.pointee.tag.has_field = true
                data.pointee.tag.v = tag_val
            } else {
                data.pointee.tag.has_field = false
            }
            data.pointee.is_reference = self.isReference != nil
        }
    }

    public class Structure : iopsq_package.PackageElem {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__structure__sp
        }

        open override class var isAbstract : Swift.Bool {
            return true
        }


        public override init(`name`: Swift.String) {
            super.init(name: `name`)
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            super.fill(c, on: allocator)
        }
    }

    public final class Struct : iopsq_package.Structure {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__struct__sp
        }

        open override class var isAbstract : Swift.Bool {
            return false
        }

        public var `fields` : [iopsq_package.Field]

        public init(`name`: Swift.String,
                    `fields`: [iopsq_package.Field] = []) {
            self.fields = `fields`
            super.init(name: `name`)
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__struct__t.self, capacity: 1)
            self.fields = try data.pointee.fields.buffer.map {
                var fields_var = $0
                return try iopsq_package.Field(&fields_var)
                }
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__struct__t.self, capacity: 1)
            libcommon.duplicate(complexTypeArray: self.fields, to: &data.pointee.fields, on: allocator)
            super.fill(c, on: allocator)
        }
    }

    public final class EnumVal : libcommon.IopStruct {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__enum_val__sp
        }

        public var `name` : Swift.String
        public var `val` : Swift.Int32?

        public init(`name`: Swift.String,
                    `val`: Swift.Int32? = nil) {
            self.name = `name`
            self.val = `val`
            super.init()
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__enum_val__t.self, capacity: 1)
            self.name = Swift.String(data.pointee.name) ?? ""
            if data.pointee.val.has_field {
                self.val = data.pointee.val.v
            }
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__enum_val__t.self, capacity: 1)
             data.pointee.name = self.name.duplicated(on: allocator)
            if let val_val = self.val {
                data.pointee.val.has_field = true
                data.pointee.val.v = val_val
            } else {
                data.pointee.val.has_field = false
            }
        }
    }

    public final class Enum : iopsq_package.PackageElem {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__enum__sp
        }

        open override class var isAbstract : Swift.Bool {
            return false
        }

        public var `values` : [iopsq_package.EnumVal]

        public init(`name`: Swift.String,
                    `values`: [iopsq_package.EnumVal] = []) {
            self.values = `values`
            super.init(name: `name`)
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__enum__t.self, capacity: 1)
            self.values = try data.pointee.values.buffer.map {
                var values_var = $0
                return try iopsq_package.EnumVal(&values_var)
                }
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__enum__t.self, capacity: 1)
            libcommon.duplicate(complexTypeArray: self.values, to: &data.pointee.values, on: allocator)
            super.fill(c, on: allocator)
        }
    }

    public final class Union : iopsq_package.Structure {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__union__sp
        }

        open override class var isAbstract : Swift.Bool {
            return false
        }

        public var `fields` : [iopsq_package.Field]

        public init(`name`: Swift.String,
                    `fields`: [iopsq_package.Field] = []) {
            self.fields = `fields`
            super.init(name: `name`)
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__union__t.self, capacity: 1)
            self.fields = try data.pointee.fields.buffer.map {
                var fields_var = $0
                return try iopsq_package.Field(&fields_var)
                }
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__union__t.self, capacity: 1)
            libcommon.duplicate(complexTypeArray: self.fields, to: &data.pointee.fields, on: allocator)
            super.fill(c, on: allocator)
        }
    }

    public final class Package : libcommon.IopStruct {
        open override class var descriptor : Swift.UnsafePointer<iop_struct_t> {
            return iopsq__package__sp
        }

        public var `name` : Swift.String
        public var `elems` : [iopsq_package.PackageElem]

        public init(`name`: Swift.String,
                    `elems`: [iopsq_package.PackageElem] = []) {
            self.name = `name`
            self.elems = `elems`
            super.init()
        }

        public required init(_ c: Swift.UnsafeRawPointer) throws {
            let data = c.bindMemory(to: iopsq__package__t.self, capacity: 1)
            self.name = Swift.String(data.pointee.name) ?? ""
            self.elems = try data.pointee.elems.buffer.map {                return try iopsq_package.PackageElem.make(Swift.UnsafeRawPointer($0))}
            try super.init(c)
        }

        open override func fill(_ c: Swift.UnsafeMutableRawPointer, on allocator: libcommon.FrameBasedAllocator) {
            let data = c.bindMemory(to: iopsq__package__t.self, capacity: 1)
             data.pointee.name = self.name.duplicated(on: allocator)
            libcommon.duplicate(classArray: self.elems, to: &data.pointee.elems, on: allocator)
        }
    }

    public enum interfaces {
    }
    public enum modules {
    }
    public static let classes : [libcommon.IopClass.Type] = [ PackageElem.self, Structure.self, Struct.self, Enum.self, Union.self ]
}

public typealias iopsq_package = iopsq
